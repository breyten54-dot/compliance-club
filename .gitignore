node_modules
.env
.env.local
.vercel

.env*
!.env.test.example

# Test fixture files generated during download tests (real templates are committed)
src/content/templates/fixture-*.docx

# keep the template tracked (no secrets in it)
!.env.example
